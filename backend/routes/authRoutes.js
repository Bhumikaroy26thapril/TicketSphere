const express = require('express');
const { checkAuth } = require('../controllers/authController');

const router = express.Router();

// Authentication routes can go here if needed

module.exports = router;
